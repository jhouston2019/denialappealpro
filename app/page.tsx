import LandingPro from "@/components/landing/landing-pro";

/** Public marketing home. Visiting `/` always shows the landing page (no auto-redirect to `/start`). */
export default function HomePage() {
  return <LandingPro />;
}
